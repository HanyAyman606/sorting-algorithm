package com.example.sortingalgorithm.tracker;

public interface EinSehrya {
    void compare(int i, int j);
    void swap(int i, int j);
    void overwrite(int index, int value);
    void it_is_sorted(int index);
    void pivot(int index);
}